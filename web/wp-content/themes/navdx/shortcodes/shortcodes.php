<?php
/**
 * This file is entirely for including shortcodes
 *
 * When creating new shortcodes in this directory, copy the "sampleshortcode" directory and follow the instructions at the top of load.php, then wysiwyg.js. Change the name of the sampleshortcode directory to match the name of your shortcode.
 * Then include the shortcode using the same syntax as the sample below, replacing the directory name with your shortcode's directory.
 */

$cur_dir = dirname( __FILE__ );

// Sample Shortcode
// include_once $cur_dir . '/sampleshortcode/load.php';